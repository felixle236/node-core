import { {PascalName} } from '../../../models/{PascalName}';

export class {PascalName}Response {
    id: number;
    createdAt: Date;
    updatedAt: Date;
    name: string;

    constructor(model: {PascalName}) {
        this.id = model.id;
        this.createdAt = model.createdAt;
        this.updatedAt = model.updatedAt;
        this.name = model.name;
    }
}
